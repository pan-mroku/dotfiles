[merge]
	tool = kdiff3
[mergetool "diffuse"]
	cmd = "diffuse -b"		
[push]
	default = tracking
[color]
	ui = auto
[alias]
	diffw = diff -w
	l = log --graph
	lp = log --graph --follow -p -w
	lc = log -w --follow --pretty=format:'%h %<(20)%Cred%an %Cgreen%s'
	changes = log -w --pretty=format:\"%h\"
	s = status -sb
[log]
	decorate = true
